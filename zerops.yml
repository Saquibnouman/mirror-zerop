zerops:
  - setup: app

    build:
      base: nodejs@20
      buildCommands:
        - npm ci
      deployFiles:
        - ./
      cache:
        - node_modules

    run:
      base: nodejs@20

    ports:
      - port: 3000
        httpSupport: true

    start: node server.js
